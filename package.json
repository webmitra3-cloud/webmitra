{
  "name": "webmitra-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w web",
    "seed": "npm run seed -w web",
    "lint": "npm run lint -w web"
  }
}
